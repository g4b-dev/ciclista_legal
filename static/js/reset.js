document.getElementById('resetForm').addEventListener('submit', function(event) {
    event.preventDefault();
    // Aqui vai a lógica de redefinição de senha
    alert('Instruções de redefinição de senha enviadas para o seu email!');
});
